# PCcUpdater

Telegram-бот для обновления удаленного управления с помощью:

- последнего релиза GitHub;
- загрузки `.zip` файла в чат.

## Сборка

Требования:

- CMake 3.20+
- компилятор с поддержкой C++17
- libcurl (для HTTP запросов)

Пример сборки:

```bash
cmake -S . -B build
cmake --build build
```

## Быстрый старт

1. Задайте переменные окружения:

```powershell
$env:TELEGRAM_BOT_TOKEN="<bot_token>"
$env:PC_UPDATER_TARGET_DIR="C:\Users\Gleb\pc"
$env:PC_UPDATER_GITHUB_REPO="owner/repo"
```

2. (Опционально) Укажите exe для перезапуска и автозапуска, если в папке несколько `.exe`:

```powershell
$env:PC_UPDATER_TARGET_EXE="RemoteControl.exe"
$env:PC_UPDATER_AUTOSTART_NAME="RemoteControl"
```

3. Запустите бота:

```bash
./build/pc_updater
```

## Управление ботом

- В главном меню нажмите **Обновить**.
- Выберите способ:
  - **GitHub-ом** — скачивает последний релиз GitHub и обновляет файлы.
  - **.zip-ом** — ожидает `.zip` файл в чат и обновляет файлы.
  - **Выйти** — возвращает в главное меню.

## Как происходит обновление

1. `.zip` скачивается в системную временную директорию.
2. Архив распаковывается во временную папку.
3. Перед установкой выполняется тестовый запуск `.exe` из обновления.
4. Содержимое папки управления синхронизируется с архивом:
   - файлы, которых нет в архиве, удаляются;
   - новые файлы добавляются;
   - существующие перезаписываются.
5. После обновления exe перезапускается и заново добавляется в автозапуск.

## Дополнительно

- Для GitHub можно использовать токен доступа:

```powershell
$env:PC_UPDATER_GITHUB_TOKEN="ghp_..."
```

- Бот принимает `.zip` только в личных сообщениях.
